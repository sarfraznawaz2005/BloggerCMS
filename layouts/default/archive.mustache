{{> head}}
<body>
{{> nav}}

<div class="container">
    <div class="row">
        <div class="col-md-8 main-content">
            <h1>Monthly Posts</h1>
            <hr/>
            <br/>

            {{#archivePosts}}
                <h3><a href="{{settings.url}}/post/{{slug}}">{{title}}</a></h3>
                <span class="glyphicon glyphicon-time"></span> {{dated}}
            {{/archivePosts}}
        </div>

        {{> sidebar}}
    </div>
</div>

{{> footer}}